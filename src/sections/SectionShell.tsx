import styled from 'styled-components';

export const SectionShell = styled.div`
  display: grid;
  gap: 12px;

  h2 {
    font-size: 18px;
    font-weight: 600;
    margin-bottom: 4px;
  }

  ul {
    padding-left: 18px;
    display: grid;
    gap: 6px;
  }
`;
